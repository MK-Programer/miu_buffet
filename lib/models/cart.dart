class Cart {
  String uid;
  int pid;
  String picture;
  String name;
  String sugar;
  int quantity;
  String price;
  String totalPrice;
  String category;

  Cart(this.uid, this.pid, this.picture, this.name, this.sugar, this.quantity,
      this.price, this.totalPrice, this.category);
}
